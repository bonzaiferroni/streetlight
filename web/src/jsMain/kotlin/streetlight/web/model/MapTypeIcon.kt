package streetlight.web.model

import koala.SvgFile

/** The icon of a location marker by its map type. */
val MapTypeIcon = mapOf(
    "Restaurant" to SvgFile.Food,
    "Ice cream" to SvgFile.Food,
)