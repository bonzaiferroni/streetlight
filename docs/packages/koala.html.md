# koala.html

Server-renderable components declared on `FlowContent`.

## Route Menu Classes

`RouteMenu` holds the classes the route dock in `streetlight.web.ui` applies to its routes. `NavMenuCss` is its stylesheet.
